[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-quantum-canary"
version = "0.1.0"
description = "Polyformalism canary via quantum amplitude"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_quantum_canary"]
